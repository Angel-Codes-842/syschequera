# Sistema de Emisión de Cheques - Dependencias del Sistema de Cheques
# Python 3.10+

# Generación de PDFs
reportlab>=4.0.0,<5.0.0

# Selector de fecha (opcional, usa fallback si no está)
tkcalendar>=1.6.0,<2.0.0

# Funciones de Windows (solo Windows)
pywin32>=305

# Testing (solo desarrollo)
pytest>=7.4.0,<8.0.0; sys_platform == 'win32'

# Testing
pytest>=7.0.0,<8.0.0
